# VITE_API_URL intentionally empty in prod: el front pega a /api (relativo) y Netlify proxea al backend.
# Sin esto, Vite hereda el VITE_API_URL del .env de dev (localhost:8600) y lo hornea en el bundle.
VITE_API_URL=
VITE_APP_NAME=TasAR
